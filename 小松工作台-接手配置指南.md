# 小松工作台 — 接手配置指南
> 用于让另一个人或 AI 快速接手编辑工作台。阅读本文档 + `index.html` 源码即可上手。
> 最后更新：2026-08-25

---

## 一、项目信息
- **项目名称**：小松的工作台 · 男生版
- **仓库地址**：https://github.com/xiaosong2011/ajie-workbench
- **线上地址**：https://xiaosong2011.github.io/ajie-workbench/
- **主文件**：`index.html`（纯前端单文件，约 9400+ 行）
- **代理服务**：`server.py`（本地代理，用于网易云音乐歌单导入，端口 8080）
- **技术栈**：HTML5 + CSS3 + 原生 JavaScript，无构建工具，无后端
- **数据存储**：localStorage 本地存储 + Supabase 云同步（可选）

---

## 二、外部依赖（CDN）
```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="https://cdn.jsdelivr.net/gh/davidshimjs/qrcodejs/qrcode.min.js"></script>
```
- Supabase JS SDK — 云数据同步
- QRCode.js — 分享二维码生成

---

## 三、核心配置常量（代码结构）

所有功能集中在以下常量中，修改功能只需改对应常量：

### 1. `NAV` — 导航菜单结构
位置：约第 1780 行
```
分组：核心日常
  📅 daily     — 每日计划
  ⭐ inspo     — 灵感收藏（含飞书漫剧库）
  🎬 workshop  — AI漫剧工坊
  🤖 agent     — 智能体库
分组：财务理财
  💰 salary    — 工资储蓄
  📊 ledger    — 小账本
  🏦 repay     — 还款提醒
  🧾 bills     — 房租水电
  💼 hustle    — 副业搞钱
分组：学习成长
  🎥 selfmedia — 自媒体学习
  🤖 aitech    — AI技术学习
  🧠 ai        — AI学习
分组：生活管理
  💪 fit       — 力量训练
  🧔 groom     — 理容穿搭
  🎬 anime     — 动漫追剧
  🎤 music     — KTV歌单
分组：系统
  💾 data      — 数据管理
```

### 2. `LISTS` — 列表配置（CRUD 驱动）
位置：约第 1850 行
每个列表定义 `title/icon/dbKey/fields`，可选 `copyField`、`clickCopy`。

核心列表与之前相同，新增：
| 列表key | 标题 | dbKey | 特殊配置 | 字段 |
|---------|------|-------|----------|------|
| scriptProjects | 漫剧项目 | scriptProjects | - | title, desc, tag |

### 3. `SECTION_LISTS` — 板块→列表映射
```javascript
const SECTION_LISTS = {
  fit:       ['fitVideos', 'fitRecords'],
  groom:     ['groomRoutine', 'groomOutfit'],
  hustle:    ['hustleProjects'],
  ai:        ['aiPath', 'aiTools'],
  weather:   ['weatherNotes'],
  anime:     ['animeTracking', 'animeWatchlist', 'animeFav'],
  salary:    ['salaryIncome', 'salaryGoal', 'salaryAlloc'],
  repay:     ['repayList', 'repayLog'],
  music:     ['ktvSongs', 'musicPlaylist', 'musicNew'],
  selfmedia: ['smCases', 'smTopics', 'smScripts', 'smNotes'],
  aitech:    ['aitechPrompts', 'aitechComfy', 'aitechModels', 'aitechNotes'],
  bills:     ['billsRent', 'billsUtility', 'billsFixed'],
};
```

### 4. `SECTIONS` — 板块 HTML 模板
位置：约第 2240 行
每个板块的 HTML 结构，包含卡片容器 `#lc_<列表key>`。

### 5. `INITIAL` — 初始示例数据
位置：约第 2950 行
首次加载时填充的示例数据，包含大量漫剧角色描述。

---

## 四、AI 漫剧工坊（核心功能）

位置：约第 2240 行（SECTIONS.workshop）

### 功能概述
- 输入小说文本 → AI 自动生成分镜提示词
- 支持多种画风（国漫写实、韩漫、二次元、古风厚涂等）
- 支持多种小说类型（古风仙侠、都市、悬疑等）
- 三种生成模式：完整解析、快速生成分镜、仅解析设定
- 生成结果包含：角色设定、场景描述、分镜提示词、完整提示词

### 关键函数
| 函数名 | 作用 |
|--------|------|
| `wsGenerate()` | 触发AI生成漫剧提示词 |
| `wsBuildSystemPrompt(mode)` | 构建系统提示词 |
| `wsRenderResult(data,style,novelType)` | 渲染生成结果 |
| `wsExtractChars(data,mode)` | 提取角色设定 |
| `wsExtractScenes(data,mode)` | 提取场景描述 |
| `wsExtractPanels(data,mode)` | 提取分镜提示词 |
| `wsExtractFullPrompt(data,mode)` | 提取完整提示词 |

### 输入限制
- 小说文本最大输入：20000 字（超出自动截断）
- AI输出限制：`max_tokens: 16384`

---

## 五、智能体库（API 配置中心）

位置：约第 2360 行（SECTIONS.agent）

### 功能概述
- 管理 AI API 配置（服务商、API地址、Key、模型）
- 快速切换服务商（豆包、智谱AI、DeepSeek、自定义）
- 支持漫剧剧本生成、角色对话等功能

### 服务商配置
```javascript
var PROVIDER_PRESETS={
  doubao:{baseUrl:'https://ark.cn-beijing.volces.com/api/v3',model:'ep-xxx',name:'豆包大模型'},
  zhipu:{baseUrl:'https://open.bigmodel.cn/api/paas/v4',model:'glm-4-plus',name:'智谱AI'},
  deepseek:{baseUrl:'https://api.deepseek.com/v1',model:'deepseek-chat',name:'DeepSeek'}
};
```

### 当前推荐：豆包 Doubao-Seed-2.1-pro
- **服务商**：火山引擎方舟
- **API地址**：`https://ark.cn-beijing.volces.com/api/v3`
- **模型**：通过 Endpoint ID 调用（如 `ep-20260825182529-q7kr2`）
- **免费额度**：每个模型 50 万 tokens
- **API Key**：用户在智能体库页面手动填入（不硬编码在代码中）

### 关键函数
| 函数名 | 作用 |
|--------|------|
| `switchProvider(provider)` | 切换服务商预设 |
| `saveAgentApiSettings()` | 保存API设置 |
| `testAgentApi()` | 测试API连接 |
| `callAgentLLM(systemPrompt,userMessage,useJson)` | 调用AI模型 |
| `generateManhuaScript()` | 生成漫剧剧本 |

### API 调用逻辑
- 代码位置：约第 7430 行 和 第 9160 行（两处调用点）
- 请求格式：OpenAI 兼容的 `/chat/completions` 接口
- 支持 JSON 模式返回（`response_format:{type:'json_object'}`）
- 输出限制：`max_tokens: 16384`

---

## 六、飞书漫剧库集成

### 架构概览
由于浏览器 CORS 限制，无法直接调用飞书 API。采用 GitHub Actions 定时同步方案：
```
飞书多维表格 → GitHub Actions(Python) → feishu-manju.json → 前端读取
```

### 文件结构
| 文件 | 说明 |
|------|------|
| `scripts/sync_feishu_manju.py` | Python 同步脚本，从飞书拉取数据 |
| `.github/workflows/sync-feishu.yml` | GitHub Actions 工作流，每小时执行 |
| `feishu-manju.json` | 同步生成的数据文件，前端直接读取 |

### 飞书配置
位置：约第 6000 行
```javascript
var FEISHU_CFG_KEY='ajie_feishu_cfg';
var FEISHU_DEFAULT={
  appId:'cli_aa060bcde2781bb7',
  appSecret:'***',  // 用户手动配置
  baseToken:'JWCtb1kZMaGb7msavFNcMcTen9g',
  tableId:'tblH1sYiTwVVGygZ'
};
```

### GitHub Actions Secrets（在仓库 Settings → Secrets 中配置）
| Secret名称 | 说明 |
|-----------|------|
| `FEISHU_APP_ID` | 飞书应用 App ID |
| `FEISHU_APP_SECRET` | 飞书应用 App Secret |
| `FEISHU_BASE_TOKEN` | 多维表格 Base Token |
| `FEISHU_TABLE_ID` | 数据表 ID |

### 前端数据加载逻辑
- **页面加载时**：`loadFeishuManjuFromLocal()` 优先从 `feishu-manju.json` 加载，失败则用 localStorage 缓存
- **手动刷新**：`syncFeishuManju()` 从 JSON 文件重新加载
- **下拉刷新**：全局下拉刷新触发 `syncFeishuManju()`

### 关键函数
| 函数名 | 作用 |
|--------|------|
| `fetchFeishuManju()` | 从 feishu-manju.json 获取数据 |
| `renderFeishuManju(items)` | 渲染漫剧卡片列表 |
| `loadFeishuManjuFromLocal()` | 页面加载时初始化数据 |
| `syncFeishuManju()` | 手动刷新数据 |
| `copyFeishuManju(idx)` | 复制漫剧信息 |
| `loadFeishuCfg()` | 加载飞书配置 |
| `saveFeishuCfg(cfg)` | 保存飞书配置 |
| `openFeishuConfig()` | 打开飞书配置弹窗 |

### 漫剧库样式
- 卡片式布局，最小宽度 155px，自动排列
- 最大高度 420px 可滚动区域
- 自动显示所有飞书字段（动态遍历）
- 渐变色卡片头部，悬停上浮效果
- 顶部显示数量统计和更新时间

---

## 七、全局下拉刷新

位置：约第 6147 行

### 功能
- 所有子页面支持手势下拉刷新
- 触发对应页面的刷新函数
- 灵感收藏页面触发飞书漫剧数据刷新

### 关键函数
| 函数名 | 作用 |
|--------|------|
| `initPullRefresh()` | 初始化下拉刷新 |
| 触摸事件 | touchstart/touchmove/touchend |

---

## 八、语音输入（Web Speech API）

位置：约第 6267 行

### 功能
- 支持语音输入文字（灵感收藏标题和描述）
- 使用浏览器原生 Web Speech API
- 支持 Chrome、Edge、Safari

### 关键函数
| 函数名 | 作用 |
|--------|------|
| `toggleVoice(targetId,btnEl)` | 开启/关闭语音识别 |
| 事件 | onresult/onerror/onend |

---

## 九、数据层与云同步

### 存储键
```javascript
const STORE_KEY = 'ajie_workbench_data';    // localStorage 数据键
const CLOUD_KEY = 'ajie_cloud_config';      // 云端配置键
const FEISHU_CFG_KEY = 'ajie_feishu_cfg';   // 飞书配置键
```

### Supabase 配置（用户在「数据管理」页面填写）
- **Project URL**：`https://xxxxxxxx.supabase.co`
- **Anon Key**：`sb_publishable_...` 开头
- **工作区 ID**：自定义字符串（相同 ID = 同一份数据）

### API 设置存储
```javascript
DB.apiSettings = {
  apiBaseUrl: 'https://ark.cn-beijing.volces.com/api/v3',
  apiKey: 'ark-xxx',
  model: 'ep-xxx'
};
```
> 注意：API Key 不硬编码在代码中（GitHub Push Protection 会阻止），用户在智能体库页面手动填入。

### 数据表结构
表名：`workbench_data`
| 字段 | 类型 | 说明 |
|------|------|------|
| workspace_id | text (主键) | 工作区标识 |
| data | jsonb | 完整工作台数据 |
| updated_at | timestamptz | 最后更新时间 |

---

## 十、CSS 设计系统

位置：`:root`（约第 10 行）
```css
:root {
  --side-1: #15273b;        /* 侧栏渐变起点 */
  --side-2: #1e3349;        /* 侧栏渐变终点 */
  --side-active: #2b4a6b;   /* 侧栏选中项 */
  --side-text: #c4d4e3;     /* 侧栏文字 */
  --content-bg: #eef2f7;    /* 内容区背景 */
  --card: #ffffff;          /* 卡片背景 */
  --accent: #2e86de;        /* 主强调色（蓝） */
  --accent-2: #1abc9c;      /* 次强调色（青绿） */
  --accent-warm: #e67e22;   /* 暖强调色（橙） */
  --ink: #1f2d3d;           /* 主文字色 */
  --ink-2: #51637a;         /* 次文字色 */
  --ink-3: #8a9bb0;         /* 弱文字色 */
  --line: #e3e9f0;          /* 分割线 */
  --radius: 16px;           /* 圆角 */
}
```

### 飞书漫剧库样式类
| 类名 | 作用 |
|------|------|
| `.fs-manju-grid` | 网格容器 |
| `.fs-manju-card` | 单个卡片 |
| `.fs-manju-card-head` | 卡片头部（渐变背景） |
| `.fs-manju-card-body` | 卡片内容区 |
| `.fs-manju-card-foot` | 卡片底部（复制提示） |
| `.fs-manju-field` | 字段行（标签+值） |
| `.fs-manju-scroll` | 可滚动容器（max-height:420px） |
| `.fs-manju-toolbar` | 工具栏（数量统计+更新时间） |

---

## 十一、布局结构
```
.device (最大宽度 1180px, 100vh)
├── .statusbar (顶部状态栏, 适配全面屏安全区)
├── .main (flex 分栏)
│   ├── .sidebar (左侧导航, 188px 宽, 移动端从左侧滑出抽屉)
│   │   ├── .side-head (头像 + 工作台名称)
│   │   ├── .nav-scroll (菜单列表, 按分组)
│   │   └── .sync-badge (同步状态指示)
│   └── .content (右侧内容, 可滚动)
│       └── .content-inner (内容容器, 最大 760px)
└── (底部安全区留白)
```
- 桌面端：导航固定在左侧，内容在右侧
- 移动端（≤768px）：导航为左侧抽屉，点击☰按钮或浮动菜单按钮打开
- 浮动菜单按钮（☰）位于左下角

---

## 十二、关联文件

| 文件 | 说明 |
|------|------|
| `index.html` | 小松工作台主文件（约9400+行） |
| `scripts/sync_feishu_manju.py` | 飞书数据同步Python脚本 |
| `.github/workflows/sync-feishu.yml` | GitHub Actions定时同步工作流 |
| `feishu-manju.json` | 飞书漫剧数据文件（自动生成） |
| `server.py` | 本地代理服务器（网易云音乐歌单导入） |
| `baozhuangban.html` | 包装工作台（关联使用同一 Supabase） |
| `部署指南.html` | 部署说明 |
| `Supabase配置指南.md` | Supabase 配置教程 |
| `GitHub上传指南.md` | GitHub 上传指南 |
| `gold-data.json` | 金价数据 |
| `oil-data.json` | 油价数据 |

---

## 十三、近期改动历史

| 日期 | 说明 |
|------|------|
| 2026-08-25 | 接入豆包 Doubao-Seed-2.1-pro 作为默认AI模型 |
| 2026-08-25 | 优化漫剧库样式：紧凑卡片+滚动容器+数量统计 |
| 2026-08-25 | 增大AI输出限制：max_tokens 8K→16K，输入截断8K→20K |
| 2026-08-25 | 飞书漫剧库集成：GitHub Actions定时同步方案 |
| 2026-08-25 | 前端改为从本地JSON读取飞书数据，解决CORS问题 |
| 2026-08-25 | 漫剧库卡片式重设计：渐变色头部+自动显示所有字段 |
| 2026-08-21 | 导航栏移回左侧：移动端抽屉从左侧滑出 |
| 2026-08-21 | 网易云音乐歌单导入改用本地代理服务器 |
| 2026-08-20 | 提示词库标题和内容支持点击直接复制 |
| 2026-08-20 | 灵感收藏描述默认截断2行,点击展开/收起 |
| 2026-08-20 | 灵感语音输入+点击复制，Supabase默认配置 |

---

## 十四、如何推送改动到 GitHub

### 方式 A：命令行（推荐）
```bash
cd /workspace/ajie-workbench
git add index.html
git commit -m "feat: 你的改动说明"
git push
```
> 推送后 GitHub Pages 1-2 分钟自动更新。

### 注意事项
- **不要在代码中硬编码 API Key**：GitHub Push Protection 会阻止推送
- API Key 由用户在智能体库页面手动填入，存储在 localStorage
- 飞书 App Secret 通过 GitHub Secrets 配置，不在代码中出现

---

## 十五、给接手者的提示

1. **先读本文档**，了解整体结构
2. **读取 `index.html` 源码**，重点关注 `NAV`/`LISTS`/`SECTION_LISTS`/`SECTIONS`/`INITIAL` 五个常量
3. **AI API 配置**：用户在「智能体库」页面手动填入 API Key，不硬编码在代码中
4. **飞书数据同步**：通过 GitHub Actions 每小时自动同步，前端读取 `feishu-manju.json`
5. **修改功能时**，优先用配置驱动方式（如 `copyField`/`clickCopy`），避免硬编码
6. **CSS 修改**，统一改 `:root` 变量，不要散落硬编码颜色
7. **测试方式**：浏览器直接打开 `index.html` 即可测试，无需服务器
8. **数据同步逻辑**在 `initSupabase()` / `loadFromCloud()` / `saveToCloud()` / `subscribeCloud()` / `startPolling()` 中
9. **推送前确认**：不要在代码中写 API Key 等敏感信息，GitHub 会自动拦截
10. **漫剧角色数据**：`INITIAL` 中包含大量预设角色（约40+个），修改时注意保留
