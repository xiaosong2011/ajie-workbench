# 小松工作台 — 接手配置指南
> 用于让另一个人或 AI 快速接手编辑工作台。阅读本文档 + `index.html` 源码即可上手。
---
## 一、项目信息
- **项目名称**：小松的工作台 · 男生版
- **仓库地址**：https://github.com/xiaosong2011/ajie-workbench
- **线上地址**：https://xiaosong2011.github.io/ajie-workbench/
- **主文件**：`index.html`（纯前端单文件，约 2600 行）
- **代理服务**：`server.py`（本地代理，用于网易云音乐歌单导入，端口 8080）
- **技术栈**：HTML5 + CSS3 + 原生 JavaScript，无构建工具，无后端
- **数据存储**：localStorage 本地存储 + Supabase 云同步（可选）
---
## 二、外部依赖（CDN）
```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="https://cdn.jsdelivr.net/gh/davidshimjs/qrcodejs/qrcode.min.js"></script>
```
- Supabase JS SDK — 云数据同步
- QRCode.js — 分享二维码生成
---
## 三、核心配置常量（代码结构）
所有功能集中在以下五个常量中，修改功能只需改对应常量：
### 1. `NAV` — 导航菜单结构
位置：约第 720 行
```
分组：核心日常
  📅 daily     — 每日计划（badge: 6）
  ⭐ inspo     — 灵感收藏
  ☀️ weather   — 天气
分组：财务理财
  💰 salary    — 工资储蓄
  📊 ledger    — 小账本
  🏦 repay     — 还款提醒
  🧾 bills     — 房租水电
  💼 hustle    — 副业搞钱
分组：学习成长
  🎥 selfmedia — 自媒体学习
  🤖 aitech    — AI技术学习
  🧠 ai        — AI学习
分组：生活管理
  💪 fit       — 力量训练
  🧔 groom     — 理容穿搭
  🎬 anime     — 动漫追剧
  🎤 music     — KTV歌单
分组：系统
  💾 data      — 数据管理
```
### 2. `LISTS` — 列表配置（CRUD 驱动）
位置：约第 830 行
每个列表定义 `title/icon/dbKey/fields`，可选 `copyField`、`clickCopy`。
| 列表key | 标题 | dbKey | 特殊配置 | 字段 |
|---------|------|-------|----------|------|
| fitVideos | 跟练视频 | fitVideos | - | title, desc |
| fitRecords | 力量数据 | fitRecords | - | item, weight, target, status |
| groomRoutine | 理容清单 | groomRoutine | - | title, desc |
| groomOutfit | 穿搭公式 | groomOutfit | - | scene, formula |
| hustleProjects | 副业项目 | hustleProjects | - | title, desc, tag |
| aiPath | 学习路径 | aiPath | - | title, desc |
| aiTools | 常用工具 | aiTools | - | category, title |
| weatherNotes | 自定义备注 | weatherNotes | - | title |
| animeTracking | 追番清单 | animeTracking | - | title, meta, status |
| animeWatchlist | 待看动漫 | animeWatchlist | - | title, desc |
| animeFav | 动漫收藏 | animeFav | - | title, meta, desc |
| salaryIncome | 每月收入记录 | salaryIncome | - | month, amount, note |
| salaryGoal | 储蓄目标 | salaryGoal | - | title, target, current, status |
| salaryAlloc | 资金分配 | salaryAlloc | - | category, amount, desc |
| repayList | 还款登记 | repayList | - | title, date, amount, status |
| repayLog | 还款记录 | repayLog | - | title, date, amount, note |
| ktvSongs | KTV必点歌曲 | ktvSongs | - | title, singer, note |
| musicPlaylist | 日常循环歌单 | musicPlaylist | - | title, singer |
| musicNew | 新歌收藏 | musicNew | - | title, singer, desc |
| smCases | 爆款案例 | smCases | - | title, desc, tag |
| smTopics | 选题灵感 | smTopics | - | title, desc |
| smScripts | 脚本素材 | smScripts | - | title, desc |
| smNotes | 剪辑学习笔记 | smNotes | - | title, desc |
| **aitechPrompts** | **提示词库** | **aitechPrompts** | **copyField:'desc', clickCopy:true** | title, desc, tag |
| aitechComfy | ComfyUI资料 | aitechComfy | - | title, meta, desc |
| aitechModels | 模型与工作流 | aitechModels | - | title, meta, desc |
| aitechNotes | 学习笔记 | aitechNotes | - | title, desc |
| billsRent | 房租缴费 | billsRent | - | title, amount, status, note |
| billsUtility | 水电费用 | billsUtility | - | title, amount, month |
| billsFixed | 固定生活支出 | billsFixed | - | title, amount, cycle |
### 3. `SECTION_LISTS` — 板块→列表映射
位置：约第 905 行
```javascript
const SECTION_LISTS = {
  fit:       ['fitVideos', 'fitRecords'],
  groom:     ['groomRoutine', 'groomOutfit'],
  hustle:    ['hustleProjects'],
  ai:        ['aiPath', 'aiTools'],
  weather:   ['weatherNotes'],
  anime:     ['animeTracking', 'animeWatchlist', 'animeFav'],
  salary:    ['salaryIncome', 'salaryGoal', 'salaryAlloc'],
  repay:     ['repayList', 'repayLog'],
  music:     ['ktvSongs', 'musicPlaylist', 'musicNew'],
  selfmedia: ['smCases', 'smTopics', 'smScripts', 'smNotes'],
  aitech:    ['aitechPrompts', 'aitechComfy', 'aitechModels', 'aitechNotes'],
  bills:     ['billsRent', 'billsUtility', 'billsFixed'],
};
```
### 4. `SECTIONS` — 板块 HTML 模板
位置：约第 1000 行
每个板块的 HTML 结构，包含卡片容器 `#lc_<列表key>`。
### 5. `INITIAL` — 初始示例数据
位置：约第 1350 行
首次加载时填充的示例数据。
---
## 四、特殊功能配置（本次新增）

### 批量导入功能
所有 CRUD 列表都支持批量导入，点击列表顶部的「批量导入」按钮即可打开弹窗。

支持两种导入方式：
1. **网易云音乐歌单链接**：粘贴短链接（如 `https://163cn.tv/xxx`）或完整链接，自动解析获取歌单歌曲
2. **手动文本**：每行一首歌，支持多种格式自动解析

手动文本支持格式：
```
歌曲名 - 歌手名
歌曲名（歌手名）
歌曲名【歌手】
1. 歌曲 - 歌手 备注
```

关键函数：
| 函数名 | 作用 |
|--------|------|
| `openBatchModal(listKey)` | 打开批量导入弹窗 |
| `parseBatchPreview(listKey)` | 预览解析（自动检测链接或文本） |
| `fetchNeteasePlaylist(url,listKey)` | 通过本地代理获取网易云歌单 |
| `parseSongs(text)` | 解析手动文本为歌曲数组 |
| `confirmBatchImport(listKey)` | 确认导入 |
| `_renderBatchPreview(songs,btn)` | 渲染预览列表 |

### 本地代理服务器（server.py）
位置：`/data/user/work/server.py`

用途：解决网易云音乐 API 的 CORS 限制，在本地代理请求歌单数据。

启动命令：
```bash
python3 /data/user/work/server.py
```
- 运行在端口 8080
- 同时提供静态文件服务（可访问 `http://localhost:8080/` 预览工作台）
- API 端点：`/api/playlist?url=<网易云链接>`
- 返回 JSON：`{success, songs, count, playlist_name, playlist_id}`
- 自动解析短链接 → 提取歌单ID → 调用网易云API → 返回歌曲列表

> 注意：代理服务器需保持运行，否则歌单链接解析不可用，手动文本导入不受影响。

### 点击复制功能
通过 `LISTS` 配置中的两个可选字段控制：
| 配置项 | 作用 | 示例 |
|--------|------|------|
| `copyField: '字段名'` | 指定要复制的字段，会显示「📋 复制提示词」按钮 | `copyField: 'desc'` |
| `clickCopy: true` | 标题和描述文字本身可点击复制（与灵感收藏体验一致） | `clickCopy: true` |
**当前已启用的列表：**
- `aitechPrompts` — 同时启用 `copyField:'desc'` + `clickCopy:true`
**如何给其他列表添加点击复制：**
在 `LISTS` 中对应列表的配置里加 `clickCopy: true` 即可，无需改其他代码。
### 灵感收藏描述截断
- 描述默认显示 2 行，点击展开/收起
- CSS 类：`.gdesc.clamp`（截断）、`.gitem.expanded .gdesc.clamp`（展开）
- 函数：`toggleInspoExpand(i)`
---
## 五、关键函数索引
| 函数名 | 位置(约) | 作用 |
|--------|----------|------|
| `renderList(listKey)` | 1695 | 渲染 CRUD 列表 |
| `renderListItem(listKey,item,i)` | 1726 | 渲染单条列表项（含 copyField/clickCopy 逻辑） |
| `copyListItemField(btn,listKey,i,fieldKey)` | 1618 | 复制列表项指定字段 |
| `copyText(el,customText)` | 1600 | 通用复制函数（含 toast 提示） |
| `_showCopyTip(el)` | 1628 | 显示"已复制 ✓"浮动提示 |
| `renderInspos()` | 1983 | 渲染灵感收藏列表 |
| `toggleInspoExpand(i)` | 1975 | 灵感收藏展开/收起 |
| `openListModal(listKey,index)` | 1813 | 打开 CRUD 编辑弹窗 |
| `saveListItem(listKey,index)` | 1856 | 保存列表项 |
| `delListItem(listKey,i)` | 1868 | 删除列表项 |
| `showItemMenu(e,listKey,i)` | 1772 | 显示 ⋯ 操作菜单 |
| `initSupabase()` | - | 初始化 Supabase 云同步 |
| `saveData()` | - | 保存数据到 localStorage + 云端 |
---
## 六、CSS 设计系统
位置：`:root`（约第 10 行）
```css
:root {
  --side-1: #15273b;        /* 侧栏渐变起点 */
  --side-2: #1e3349;        /* 侧栏渐变终点 */
  --side-active: #2b4a6b;   /* 侧栏选中项 */
  --side-text: #c4d4e3;     /* 侧栏文字 */
  --side-text-dim: #7d92a6; /* 侧栏次要文字 */
  --content-bg: #eef2f7;    /* 内容区背景 */
  --card: #ffffff;          /* 卡片背景 */
  --accent: #2e86de;        /* 主强调色（蓝） */
  --accent-2: #1abc9c;      /* 次强调色（青绿） */
  --accent-warm: #e67e22;   /* 暖强调色（橙） */
  --ink: #1f2d3d;           /* 主文字色 */
  --ink-2: #51637a;         /* 次文字色 */
  --ink-3: #8a9bb0;         /* 弱文字色 */
  --line: #e3e9f0;          /* 分割线 */
  --radius: 16px;           /* 圆角 */
  /* 标签颜色 */
  --tag-blue / --tag-mint / --tag-amber / --tag-violet / --tag-rose
  /* 安全区 */
  --safe-top / --safe-bottom / --statusbar-h
}
```
### 特殊样式类
| 类名 | 作用 |
|------|------|
| `.copyable` | 可点击复制元素的样式（光标变手指+点击高亮） |
| `.gdesc.clamp` | 描述截断 2 行 |
| `.gitem.expanded .gdesc.clamp` | 展开状态 |
| `.tint-blue/mint/amber/violet` | 卡片色调 |
---
## 七、数据层与云同步
### 存储键
```javascript
const STORE_KEY = 'ajie_workbench_data';    // localStorage 数据键
const CLOUD_KEY = 'ajie_cloud_config';      // 云端配置键
```
### Supabase 配置（用户在「数据管理」页面填写）
- **Project URL**：`https://xxxxxxxx.supabase.co`
- **Anon Key**：`sb_publishable_...` 开头
- **工作区 ID**：自定义字符串（相同 ID = 同一份数据）
### 数据表结构
表名：`workbench_data`
| 字段 | 类型 | 说明 |
|------|------|------|
| workspace_id | text (主键) | 工作区标识 |
| data | jsonb | 完整工作台数据 |
| updated_at | timestamptz | 最后更新时间 |
---
## 八、布局结构
```
.device (最大宽度 1180px, 100vh)
├── .statusbar (顶部状态栏, 适配全面屏安全区)
├── .main (flex 分栏)
│   ├── .sidebar (左侧导航, 188px 宽, 移动端从左侧滑出抽屉)
│   │   ├── .side-head (头像 + 工作台名称)
│   │   ├── .nav-scroll (菜单列表, 按分组)
│   │   └── .sync-badge (同步状态指示)
│   └── .content (右侧内容, 可滚动)
│       └── .content-inner (内容容器, 最大 760px)
└── (底部安全区留白)
```
- 桌面端：导航固定在左侧，内容在右侧
- 移动端（≤768px）：导航为左侧抽屉，点击☰按钮或浮动菜单按钮打开
- 浮动菜单按钮（☰）位于左下角
---
## 九、编辑指南
### 新增一个菜单板块
1. 在 `NAV` 添加项 → 指定 `id/ico/name/group`
2. 在 `SECTIONS` 添加对应 HTML 模板
3. 如需 CRUD：在 `LISTS` 添加列表配置 → 在 `SECTION_LISTS` 建映射
### 修改配色
只需修改 `:root` 中的 CSS 变量值。
### 给列表加点击复制
在 `LISTS` 对应配置加 `clickCopy: true`。
### 给列表加复制按钮
在 `LISTS` 对应配置加 `copyField: '字段名'`。
### 调整灵感收藏截断行数
修改 CSS `.gitem .gdesc.clamp` 中的 `-webkit-line-clamp:2` 数值。
---
## 十、近期改动历史
| 日期 | Commit | 说明 |
|------|--------|------|
| 2026-08-21 | `fa01745` | 导航栏移回左侧：移动端抽屉从左侧滑出，浮动菜单按钮移至左下角 |
| 2026-08-21 | `3944327` | 网易云音乐歌单导入改用本地代理服务器，修复批量导入流程 |
| 2026-08-21 | `e974c2b` | 修复保存功能、添加批量导入、网易云音乐歌单导入 |
| 2026-08-20 | `80535ce` | 提示词库标题和内容支持点击直接复制 |
| 2026-08-20 | `a651373` | 灵感收藏描述默认截断2行,点击展开/收起 |
| 2026-08-20 | `940120f` | 提示词库增加复制按钮 |
| 2026-08-20 | `c80b795` | 合并远程更新，保留本地版本 |
| 2026-08-20 | `728fdae` | 灵感语音输入+点击复制，Supabase默认配置 |
| 2026-08-20 | `5474ba3` | 短链解析增加redirect:manual读取 |
| 2026-08-20 | `224463d` | 改用proxy.cors.sh代理 |
| 2026-08-20 | `2fc12d8` | KTV歌单批量导入网易云歌单 |
| 2026-08-20 | `6ea16c2` | 灵感收藏重构为微信式聊天界面 |
| 2026-08-20 | `c750223` | API Key和Supabase配置默认内置 |
---
## 十一、如何推送改动到 GitHub
### 方式 A：网页端（推荐）
1. 打开 https://github.com/xiaosong2011/ajie-workbench/blob/main/index.html
2. 点 ✏️ → 粘贴内容 → Commit changes
### 方式 B：命令行
需要 GitHub Personal Access Token（仅 `repo` 权限）：
```bash
git clone https://github.com/xiaosong2011/ajie-workbench.git
cp <修改后的>/index.html ajie-workbench/index.html
cd ajie-workbench
git config user.name "xiaosong2011"
git config user.email "xiaosong2011@users.noreply.github.com"
git add index.html
git commit -m "feat: 你的改动说明"
git push
```
> 推送后 GitHub Pages 1-2 分钟自动更新。
---
## 十二、给接手者的提示
1. **先读本文档**，了解整体结构
2. **读取 `index.html` 完整源码**，重点关注 `NAV`/`LISTS`/`SECTION_LISTS`/`SECTIONS`/`INITIAL` 五个常量
3. **修改功能时**，优先用配置驱动方式（如 `copyField`/`clickCopy`），避免硬编码
4. **CSS 修改**，统一改 `:root` 变量，不要散落硬编码颜色
5. **测试方式**：浏览器直接打开 `index.html` 即可测试，无需服务器
6. **数据同步逻辑**在 `initSupabase()` / `loadFromCloud()` / `saveToCloud()` / `subscribeCloud()` / `startPolling()` 中
7. **推送前确认**：本地版本是否和 GitHub 上的 main 分支一致，避免覆盖他人改动
