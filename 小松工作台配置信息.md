# 小松工作台 — 完整配置信息

> 用于在新对话中继续编辑。直接将本文件内容和 `/workspace/index.html` 源码发给智能体即可。

---

## 一、文件信息

- **文件路径**：`/workspace/index.html`
- **页面标题**：小松的工作台 · 男生版
- **部署地址**：https://xiaosong2011.github.io/ajie-workbench/index.html
- **技术栈**：纯前端 HTML + CSS + JavaScript（单文件），无构建工具

---

## 二、外部依赖（CDN）

```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="https://cdn.jsdelivr.net/gh/davidshimjs/qrcodejs/qrcode.min.js"></script>
```

- Supabase JS SDK — 云数据同步
- QRCode.js — 分享二维码生成

---

## 三、导航菜单结构（NAV）

```
分组：核心日常
  📅 daily     — 每日计划（badge: 6）
  ⭐ inspo     — 灵感收藏
  ☀️ weather   — 天气

分组：财务理财
  💰 salary    — 工资储蓄
  📊 ledger    — 小账本
  🏦 repay     — 还款提醒
  🧾 bills     — 房租水电
  💼 hustle    — 副业搞钱

分组：学习成长
  🎥 selfmedia — 自媒体学习
  🤖 aitech    — AI技术学习
  🧠 ai        — AI学习

分组：生活管理
  💪 fit       — 力量训练
  🧔 groom     — 理容穿搭
  🎬 anime     — 动漫追剧
  🎤 music     — KTV歌单

分组：系统
  💾 data      — 数据管理
```

---

## 四、列表配置（LISTS）

所有 CRUD 列表采用配置驱动模式，每项定义 `title/icon/dbKey/fields`：

| 列表key | 标题 | dbKey | 字段 |
|---------|------|-------|------|
| fitVideos | 跟练视频 | fitVideos | title(标题), desc(简介) |
| fitRecords | 力量数据 | fitRecords | item(项目), weight(当前重量), target(目标), status(状态:上涨中/已达标/停滞/下降) |
| groomRoutine | 理容清单 | groomRoutine | title(事项), desc(说明) |
| groomOutfit | 穿搭公式 | groomOutfit | scene(场景), formula(穿搭公式) |
| hustleProjects | 副业项目 | hustleProjects | title(项目名), desc(说明), tag(状态:进行中/规划中/已完成/暂停) |
| aiPath | 学习路径 | aiPath | title(阶段), desc(说明) |
| aiTools | 常用工具 | aiTools | category(分类), title(工具) |
| weatherNotes | 自定义备注 | weatherNotes | title(备注) |
| animeTracking | 追番清单 | animeTracking | title(番剧名), meta(进度), status(状态:追番中/已追完/暂停/弃番) |
| animeWatchlist | 待看动漫 | animeWatchlist | title(番剧名), desc(备注) |
| animeFav | 动漫收藏 | animeFav | title(作品名), meta(类型/年份), desc(推荐理由) |
| salaryIncome | 每月收入记录 | salaryIncome | month(月份), amount(到手收入), note(备注) |
| salaryGoal | 储蓄目标 | salaryGoal | title(目标名称), target(目标金额), current(已存金额), status(状态:进行中/已达成/已暂停) |
| salaryAlloc | 资金分配 | salaryAlloc | category(类别), amount(金额/比例), desc(说明) |
| repayList | 还款登记 | repayList | title(还款项目), date(还款日), amount(还款金额), status(状态:待还款/已还款/已逾期) |
| repayLog | 还款记录 | repayLog | title(项目), date(还款日期), amount(金额), note(备注) |
| ktvSongs | KTV必点歌曲 | ktvSongs | title(歌曲名), singer(歌手), note(备注) |
| musicPlaylist | 日常循环歌单 | musicPlaylist | title(歌曲名), singer(歌手) |
| musicNew | 新歌收藏 | musicNew | title(歌曲名), singer(歌手), desc(推荐理由) |
| smCases | 爆款案例 | smCases | title(案例标题), desc(拆解分析), tag(平台:抖音/快手/B站/小红书/其他) |
| smTopics | 选题灵感 | smTopics | title(选题), desc(思路) |
| smScripts | 脚本素材 | smScripts | title(素材标题), desc(内容) |
| smNotes | 剪辑学习笔记 | smNotes | title(笔记标题), desc(内容) |
| aitechPrompts | 提示词库 | aitechPrompts | title(提示词名称), desc(提示词内容), tag(类型:人物/场景/风格/特效/其他) |
| aitechComfy | ComfyUI资料 | aitechComfy | title(资料名称), meta(来源/链接), desc(说明) |
| aitechModels | 模型与工作流 | aitechModels | title(模型/工作流名称), meta(类型), desc(说明) |
| aitechNotes | 学习笔记 | aitechNotes | title(笔记标题), desc(内容) |
| billsRent | 房租缴费 | billsRent | title(周期), amount(金额), status(状态:已缴/待缴/已逾期), note(备注) |
| billsUtility | 水电费用 | billsUtility | title(类型), amount(金额), month(月份) |
| billsFixed | 固定生活支出 | billsFixed | title(支出项目), amount(金额), cycle(周期) |

---

## 五、板块→列表映射（SECTION_LISTS）

```javascript
const SECTION_LISTS = {
  fit:       ['fitVideos', 'fitRecords'],
  groom:     ['groomRoutine', 'groomOutfit'],
  hustle:    ['hustleProjects'],
  ai:        ['aiPath', 'aiTools'],
  weather:   ['weatherNotes'],
  anime:     ['animeTracking', 'animeWatchlist', 'animeFav'],
  salary:    ['salaryIncome', 'salaryGoal', 'salaryAlloc'],
  repay:     ['repayList', 'repayLog'],
  music:     ['ktvSongs', 'musicPlaylist', 'musicNew'],
  selfmedia: ['smCases', 'smTopics', 'smScripts', 'smNotes'],
  aitech:    ['aitechPrompts', 'aitechComfy', 'aitechModels', 'aitechNotes'],
  bills:     ['billsRent', 'billsUtility', 'billsFixed'],
};
```

---

## 六、数据层与云同步配置

### 存储键

```javascript
const STORE_KEY = 'ajie_workbench_data';    // localStorage 数据键
const CLOUD_KEY = 'ajie_cloud_config';      // 云端配置键
```

### Supabase 配置

用户在"数据管理"页面手动填写以下三项：
- **Supabase 项目 URL**：格式 `https://xxxxxxxx.supabase.co`
- **Anon Key（匿名密钥）**：格式 `eyJhbGci...`
- **工作区 ID**：自定义字符串，多人共享同一 ID 即可同步

### Supabase 数据表结构

表名：`workbench_data`

| 字段 | 类型 | 说明 |
|------|------|------|
| workspace_id | text (主键) | 工作区标识 |
| data | jsonb | 完整工作台数据 |
| updated_at | timestamptz | 最后更新时间 |

### 建表 SQL

```sql
CREATE TABLE IF NOT EXISTS workbench_data (
  workspace_id TEXT PRIMARY KEY,
  data JSONB NOT NULL DEFAULT '{}'::jsonb,
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- 启用行级安全
ALTER TABLE workbench_data ENABLE ROW LEVEL SECURITY;

-- 允许匿名访问（开发用，生产环境请收紧权限）
CREATE POLICY "Allow all" ON workbench_data FOR ALL USING (true) WITH CHECK (true);

-- 启用实时订阅
ALTER PUBLICATION supabase_realtime ADD TABLE workbench_data;
```

### 同步机制

1. **初始化**：页面加载时读取 `localStorage[CLOUD_KEY]`，如有配置则创建 Supabase 客户端
2. **保存**：本地 `saveData()` → `localStorage` + 防抖 500ms → `upsert` 到云端
3. **加载**：首次连接时从云端拉取数据覆盖本地
4. **实时订阅**：通过 Supabase Realtime 监听 `postgres_changes`，远端修改自动更新本地
5. **定期轮询**：每 30 秒检查云端 `updated_at` 是否有新更新
6. **防循环**：`isRemoteUpdate` 标志位避免远端→本地→远端的循环同步

---

## 七、CSS 设计系统

```css
:root {
  --side-1: #15273b;        /* 侧栏渐变起点 */
  --side-2: #1e3349;        /* 侧栏渐变终点 */
  --side-active: #2b4a6b;   /* 侧栏选中项 */
  --side-text: #c4d4e3;     /* 侧栏文字 */
  --side-text-dim: #7d92a6; /* 侧栏次要文字 */
  --content-bg: #eef2f7;    /* 内容区背景 */
  --card: #ffffff;          /* 卡片背景 */
  --accent: #2e86de;        /* 主强调色（蓝） */
  --accent-2: #1abc9c;      /* 次强调色（青绿） */
  --accent-warm: #e67e22;   /* 暖强调色（橙） */
  --ink: #1f2d3d;           /* 主文字色 */
  --ink-2: #51637a;         /* 次文字色 */
  --ink-3: #8a9bb0;         /* 弱文字色 */
  --line: #e3e9f0;          /* 分割线 */
  --radius: 16px;           /* 圆角 */
  --shadow: 0 6px 20px rgba(20,40,70,.08);
  /* 标签颜色 */
  --tag-blue: #dbeafe;   --tag-blue-t: #1d6fb8;
  --tag-mint: #d4f1e4;  --tag-mint-t: #128a6a;
  --tag-amber: #fde8d0; --tag-amber-t: #c4771a;
  --tag-violet: #e6dcf7;--tag-violet-t: #6b3fa0;
  --tag-rose: #fbdde6;  --tag-rose-t: #b83d63;
  /* 安全区 */
  --safe-top: env(safe-area-inset-top, 0px);
  --safe-bottom: env(safe-area-inset-bottom, 0px);
  --statusbar-h: calc(38px + var(--safe-top));
}
```

### 卡片色调类

- `.tint-blue` — 蓝色调
- `.tint-mint` — 青绿色调
- `.tint-amber` — 橙色调
- `.tint-violet` — 紫色调
- `.tint-rose` — 玫瑰色调

---

## 八、布局结构

```
.device (最大宽度 1180px, 100vh)
├── .statusbar (顶部状态栏, 适配全面屏安全区)
├── .main (flex 分栏)
│   ├── .sidebar (左侧导航, 188px 宽)
│   │   ├── .side-head (头像 + 工作台名称)
│   │   ├── .nav-scroll (菜单列表, 按分组)
│   │   └── .sync-badge (同步状态指示)
│   └── .content (右侧内容, 可滚动)
│       └── .content-inner (内容容器, 最大 760px)
└── (底部安全区留白)
```

---

## 九、功能清单

### 核心功能
- **每日计划**：Todo 待办 + 日历签到打卡
- **灵感收藏**：快速记录灵感（标题 + 内容）
- **天气**：天气信息 + 自定义备注
- **小账本**：收支记录 + 月度预算

### CRUD 模块（配置驱动）
- 所有 `LISTS` 中的列表均支持：新增、编辑、删除
- 通用函数：`renderList()`, `openListModal()`, `saveListItem()`, `deleteListItem()`

### 数据管理
- 云同步设置（Supabase URL + Key + Workspace ID）
- 分享二维码 / 系统分享 / 复制链接
- 本地导出 / 导入 / 清空
- 实时同步状态徽章（本地模式 / 已连接 / 同步中 / 错误）

---

## 十、初始数据（INITIAL）

页面首次加载时填充的示例数据，包含：
- `todos`：6 条待办事项
- `inspos`：灵感收藏
- `ledger`：账本记录
- `checkedDays`：签到天数
- `budget`：预算（默认 6000）
- 各 CRUD 列表均有示例数据

---

## 十一、关联文件

| 文件 | 说明 |
|------|------|
| `/workspace/index.html` | 小松工作台主文件 |
| `/workspace/baozhuangban.html` | 包装工作台（关联使用同一 Supabase） |
| `/workspace/部署指南.html` | 部署说明 |
| `/workspace/Supabase配置指南.md` | Supabase 配置教程 |

---

## 十二、给智能体的提示

如果要继续编辑小松工作台，请：
1. 读取 `/workspace/index.html` 完整文件
2. 所有配置集中在 `NAV`、`LISTS`、`SECTION_LISTS`、`SECTIONS`、`INITIAL` 五个常量中
3. 新增菜单：在 `NAV` 添加项 → 在 `SECTIONS` 添加对应 HTML → 如需 CRUD 在 `LISTS` 添加配置并在 `SECTION_LISTS` 映射
4. 数据同步逻辑在 `initSupabase()` / `loadFromCloud()` / `saveToCloud()` / `subscribeCloud()` / `startPolling()` 中
5. CSS 变量在 `:root` 中统一管理，修改配色只需改变量值
