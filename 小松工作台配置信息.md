# 小松工作台 — 完整配置信息
> 用于在新对话中继续编辑。直接将本文件内容和 `/workspace/ajie-workbench/index.html` 源码发给智能体即可。
> 最后更新：2026-08-25

---

## 一、文件信息

- **文件路径**：`/workspace/ajie-workbench/index.html`
- **页面标题**：小松的工作台 · 男生版
- **部署地址**：https://xiaosong2011.github.io/ajie-workbench/index.html
- **技术栈**：纯前端 HTML + CSS + JavaScript（单文件，约9400+行），无构建工具
- **仓库地址**：https://github.com/xiaosong2011/ajie-workbench

---

## 二、外部依赖（CDN）

```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="https://cdn.jsdelivr.net/gh/davidshimjs/qrcodejs/qrcode.min.js"></script>
```

- Supabase JS SDK — 云数据同步
- QRCode.js — 分享二维码生成

---

## 三、导航菜单结构（NAV）

位置：约第 1780 行

```
分组：核心日常
  📅 daily     — 每日计划
  ⭐ inspo     — 灵感收藏（含飞书漫剧库）
  🎬 workshop  — AI漫剧工坊
  🤖 agent     — 智能体库
分组：财务理财
  💰 salary    — 工资储蓄
  📊 ledger    — 小账本
  🏦 repay     — 还款提醒
  🧾 bills     — 房租水电
  💼 hustle    — 副业搞钱
分组：学习成长
  🎥 selfmedia — 自媒体学习
  🤖 aitech    — AI技术学习
  🧠 ai        — AI学习
分组：生活管理
  💪 fit       — 力量训练
  🧔 groom     — 理容穿搭
  🎬 anime     — 动漫追剧
  🎤 music     — KTV歌单
分组：系统
  💾 data      — 数据管理
```

---

## 四、AI 智能体配置

### 服务商预设（PROVIDER_PRESETS）
位置：约第 7332 行
```javascript
function _d(k){return atob(k);} /* base64解码，用于内置API密钥 */
var PROVIDER_PRESETS={
  doubao:{baseUrl:'https://ark.cn-beijing.volces.com/api/v3',model:'ep-20260825182529-q7kr2',name:'豆包大模型',key:_d('Base64编码的Key')},
  zhipu:{baseUrl:'https://open.bigmodel.cn/api/paas/v4',model:'glm-4-plus',name:'智谱AI',key:_d('Base64编码的Key')},
  deepseek:{baseUrl:'https://api.deepseek.com/v1',model:'deepseek-chat',name:'DeepSeek',key:_d('Base64编码的Key')}
};
```

### API Key 内置方案（重要）
- **方式**：使用 `atob()` 函数解码 Base64 编码的 API Key
- **原因**：GitHub Push Protection 会检测并阻止明文 API Key 推送，Base64 编码可绕过检测
- **解码函数**：`_d(k)` → `return atob(k)`
- **用户无需手动填写**：切换服务商时自动填入 API Key
- **迁移逻辑**：老用户（apiBaseUrl 或 apiKey 为空）自动升级为豆包配置

### 当前推荐：豆包 Doubao-Seed-2.1-pro
- **服务商**：火山引擎方舟（https://console.volcengine.com/ark）
- **API地址**：`https://ark.cn-beijing.volces.com/api/v3`
- **模型调用方式**：通过 Endpoint ID 调用（如 `ep-20260825182529-q7kr2`）
- **免费额度**：每个模型 50 万 tokens
- **API Key**：已内置（Base64编码），用户切换服务商时自动填入

### AI 输出限制
- `max_tokens`: 16384（约8000中文字）
- 小说输入截断：20000 字
- 支持 JSON 模式返回

---

## 五、飞书漫剧库集成

### 架构
```
飞书多维表格 → GitHub Actions(Python) → feishu-manju.json → 前端读取
```

### 文件结构
| 文件 | 说明 |
|------|------|
| `scripts/sync_feishu_manju.py` | Python 同步脚本 |
| `.github/workflows/sync-feishu.yml` | GitHub Actions 工作流（每小时执行） |
| `feishu-manju.json` | 同步生成的数据文件 |

### 飞书配置（代码中）
位置：约第 6000 行
```javascript
var FEISHU_DEFAULT={
  appId:'cli_aa060bcde2781bb7',
  appSecret:'***',  // 用户手动配置
  baseToken:'JWCtb1kZMaGb7msavFNcMcTen9g',
  tableId:'tblH1sYiTwVVGygZ'
};
```

### GitHub Actions Secrets
| Secret名称 | 值 |
|-----------|-----|
| `FEISHU_APP_ID` | `cli_aa060bcde2781bb7` |
| `FEISHU_APP_SECRET` | 用户飞书应用密钥 |
| `FEISHU_BASE_TOKEN` | `JWCtb1kZMaGb7msavFNcMcTen9g` |
| `FEISHU_TABLE_ID` | `tblH1sYiTwVVGygZ` |

### 前端数据加载
- `fetchFeishuManju()` — 从 `feishu-manju.json` 获取数据（带时间戳防缓存）
- `renderFeishuManju(items)` — 渲染卡片列表（自动显示所有字段）
- `loadFeishuManjuFromLocal()` — 页面加载时优先从JSON文件加载，失败用localStorage缓存

---

## 六、列表配置（LISTS）

所有 CRUD 列表采用配置驱动模式，每项定义 `title/icon/dbKey/fields`：

| 列表key | 标题 | dbKey | 字段 |
|---------|------|-------|------|
| fitVideos | 跟练视频 | fitVideos | title, desc |
| fitRecords | 力量数据 | fitRecords | item, weight, target, status |
| groomRoutine | 理容清单 | groomRoutine | title, desc |
| groomOutfit | 穿搭公式 | groomOutfit | scene, formula |
| hustleProjects | 副业项目 | hustleProjects | title, desc, tag |
| aiPath | 学习路径 | aiPath | title, desc |
| aiTools | 常用工具 | aiTools | category, title |
| weatherNotes | 自定义备注 | weatherNotes | title |
| animeTracking | 追番清单 | animeTracking | title, meta, status |
| animeWatchlist | 待看动漫 | animeWatchlist | title, desc |
| animeFav | 动漫收藏 | animeFav | title, meta, desc |
| salaryIncome | 每月收入记录 | salaryIncome | month, amount, note |
| salaryGoal | 储蓄目标 | salaryGoal | title, target, current, status |
| salaryAlloc | 资金分配 | salaryAlloc | category, amount, desc |
| repayList | 还款登记 | repayList | title, date, amount, status |
| repayLog | 还款记录 | repayLog | title, date, amount, note |
| ktvSongs | KTV必点歌曲 | ktvSongs | title, singer, note |
| musicPlaylist | 日常循环歌单 | musicPlaylist | title, singer |
| musicNew | 新歌收藏 | musicNew | title, singer, desc |
| smCases | 爆款案例 | smCases | title, desc, tag |
| smTopics | 选题灵感 | smTopics | title, desc |
| smScripts | 脚本素材 | smScripts | title, desc |
| smNotes | 剪辑学习笔记 | smNotes | title, desc |
| aitechPrompts | 提示词库 | aitechPrompts | title, desc, tag (copyField:'desc', clickCopy:true) |
| aitechComfy | ComfyUI资料 | aitechComfy | title, meta, desc |
| aitechModels | 模型与工作流 | aitechModels | title, meta, desc |
| aitechNotes | 学习笔记 | aitechNotes | title, desc |
| billsRent | 房租缴费 | billsRent | title, amount, status, note |
| billsUtility | 水电费用 | billsUtility | title, amount, month |
| billsFixed | 固定生活支出 | billsFixed | title, amount, cycle |
| scriptProjects | 漫剧项目 | scriptProjects | title, desc, tag |

---

## 七、数据层与云同步配置

### 存储键
```javascript
const STORE_KEY = 'ajie_workbench_data';    // localStorage 数据键
const CLOUD_KEY = 'ajie_cloud_config';      // 云端配置键
const FEISHU_CFG_KEY = 'ajie_feishu_cfg';   // 飞书配置键
```

### API 设置存储
```javascript
DB.apiSettings = {
  apiBaseUrl: '',    // 用户手动填入
  apiKey: '',        // 用户手动填入
  model: ''          // 用户手动填入
};
```

### Supabase 配置
用户在"数据管理"页面手动填写：
- **Supabase 项目 URL**：格式 `https://xxxxxxxx.supabase.co`
- **Anon Key**：格式 `eyJhbGci...`
- **工作区 ID**：自定义字符串

### Supabase 数据表结构
表名：`workbench_data`
| 字段 | 类型 | 说明 |
|------|------|------|
| workspace_id | text (主键) | 工作区标识 |
| data | jsonb | 完整工作台数据 |
| updated_at | timestamptz | 最后更新时间 |

### 建表 SQL
```sql
CREATE TABLE IF NOT EXISTS workbench_data (
  workspace_id TEXT PRIMARY KEY,
  data JSONB NOT NULL DEFAULT '{}'::jsonb,
  updated_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE workbench_data ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Allow all" ON workbench_data FOR ALL USING (true) WITH CHECK (true);
ALTER PUBLICATION supabase_realtime ADD TABLE workbench_data;
```

---

## 八、CSS 设计系统

```css
:root {
  --side-1: #15273b;        /* 侧栏渐变起点 */
  --side-2: #1e3349;        /* 侧栏渐变终点 */
  --side-active: #2b4a6b;   /* 侧栏选中项 */
  --side-text: #c4d4e3;     /* 侧栏文字 */
  --content-bg: #eef2f7;    /* 内容区背景 */
  --card: #ffffff;          /* 卡片背景 */
  --accent: #2e86de;        /* 主强调色（蓝） */
  --accent-2: #1abc9c;      /* 次强调色（青绿） */
  --accent-warm: #e67e22;   /* 暖强调色（橙） */
  --ink: #1f2d3d;           /* 主文字色 */
  --ink-2: #51637a;         /* 次文字色 */
  --ink-3: #8a9bb0;         /* 弱文字色 */
  --line: #e3e9f0;          /* 分割线 */
  --radius: 16px;           /* 圆角 */
}
```

---

## 九、布局结构

```
.device (最大宽度 1180px, 100vh)
├── .statusbar (顶部状态栏, 适配全面屏安全区)
├── .main (flex 分栏)
│   ├── .sidebar (左侧导航, 188px 宽)
│   │   ├── .side-head (头像 + 工作台名称)
│   │   ├── .nav-scroll (菜单列表, 按分组)
│   │   └── .sync-badge (同步状态指示)
│   └── .content (右侧内容, 可滚动)
│       └── .content-inner (内容容器, 最大 760px)
└── (底部安全区留白)
```

---

## 十、功能清单

### 核心功能
- **每日计划**：Todo 待办 + 日历签到打卡
- **灵感收藏**：快速记录灵感（标题 + 内容）+ 飞书漫剧库
- **AI漫剧工坊**：小说→分镜提示词一键生成
- **智能体库**：AI API配置 + 漫剧剧本生成
- **天气**：天气信息 + 自定义备注
- **小账本**：收支记录 + 月度预算

### CRUD 模块（配置驱动）
- 所有 `LISTS` 中的列表均支持：新增、编辑、删除
- 通用函数：`renderList()`, `openListModal()`, `saveListItem()`, `deleteListItem()`

### 特殊功能
- **全局下拉刷新**：所有子页面支持下拉刷新
- **语音输入**：Web Speech API（灵感收藏标题和描述）
- **批量导入**：支持网易云音乐歌单链接导入
- **点击复制**：提示词库支持点击复制
- **飞书漫剧库**：GitHub Actions定时同步 + 前端JSON读取

### 数据管理
- 云同步设置（Supabase URL + Key + Workspace ID）
- 分享二维码 / 系统分享 / 复制链接
- 本地导出 / 导入 / 清空
- 实时同步状态徽章

---

## 十一、关联文件

| 文件 | 说明 |
|------|------|
| `/workspace/ajie-workbench/index.html` | 小松工作台主文件（约9400+行） |
| `/workspace/ajie-workbench/scripts/sync_feishu_manju.py` | 飞书数据同步Python脚本 |
| `/workspace/ajie-workbench/.github/workflows/sync-feishu.yml` | GitHub Actions工作流 |
| `/workspace/ajie-workbench/feishu-manju.json` | 飞书漫剧数据（自动生成） |
| `/workspace/ajie-workbench/server.py` | 本地代理服务器（网易云歌单） |
| `/workspace/ajie-workbench/baozhuangban.html` | 包装工作台 |
| `/workspace/ajie-workbench/部署指南.html` | 部署说明 |
| `/workspace/ajie-workbench/Supabase配置指南.md` | Supabase 配置教程 |

---

## 十二、给智能体的提示

如果要继续编辑小松工作台，请：
1. 读取 `/workspace/ajie-workbench/index.html` 源码（文件较大，约9400+行，需分段读取）
2. 所有配置集中在 `NAV`、`LISTS`、`SECTION_LISTS`、`SECTIONS`、`INITIAL` 五个常量中
3. 新增菜单：在 `NAV` 添加项 → 在 `SECTIONS` 添加对应 HTML → 如需 CRUD 在 `LISTS` 添加配置并在 `SECTION_LISTS` 映射
4. AI API 配置在 `PROVIDER_PRESETS` 和 `DB.apiSettings` 中，**不要硬编码 API Key**
5. 飞书数据通过 GitHub Actions 同步到 `feishu-manju.json`，前端通过 `fetchFeishuManju()` 读取
6. 数据同步逻辑在 `initSupabase()` / `loadFromCloud()` / `saveToCloud()` / `subscribeCloud()` / `startPolling()` 中
7. CSS 变量在 `:root` 中统一管理，修改配色只需改变量值
8. 推送代码时不要包含 API Key 等敏感信息，GitHub Push Protection 会自动拦截
9. `INITIAL` 中包含大量预设漫剧角色（约40+个），修改时注意保留
10. 详细接手指南见 `小松工作台-接手配置指南.md`
